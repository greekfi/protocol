# Compiler files
cache/
out/
bin/

# Ignores development broadcast logs
/broadcast/*/31337/
/broadcast/**/dry-run/
# Keep only run-latest.json per chain; drop timestamped history
/broadcast/**/run-[0-9]*.json

# Docs
docs/

# Dotenv file
.env
localhost.json

# Scratch
.tmp/

# Yarn berry — everything under .yarn/ is machine-local (corepack fetches
# the version pinned by `packageManager` in package.json).
.yarn/
