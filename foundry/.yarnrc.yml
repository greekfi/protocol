# Yarn is managed by Corepack — version pinned via `packageManager` in package.json.

# Use classic node_modules (not PnP). Next.js, foundry's deploy scripts,
# and most tooling don't play well with PnP's strict resolver.
nodeLinker: node-modules

compressionLevel: mixed
enableColors: true
enableGlobalCache: false
